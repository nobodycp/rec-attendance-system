# === قاعدة البيانات ===
# الطريقة 1 (Coolify — موصى بها): رابط واحد
# DATABASE_URL=mysql://user:password@host:3306/default

# الطريقة 2: متغيرات منفصلة (Docker Compose محلي)
DB_DRIVER=mysql
DB_HOST=db
DB_PORT=3306
DB_NAME=rec_attendance
DB_USER=rec
DB_PASS=change_me_strong_password
DB_ROOT_PASSWORD=change_me_root_password

# للتطوير المحلي بـ SQLite (اختياري)
# DB_DRIVER=sqlite
# DB_SQLITE_PATH=/var/www/html/database/attendance.sqlite

# === التطبيق ===
APP_NAME=جمعية مركز الإرشاد التربوي REC
APP_URL=https://attendance.example.com
APP_BASE_PATH=
APP_TIMEZONE=Asia/Riyadh
APP_DEBUG=false

# فعّل مؤقتاً لإنشاء حساب المسؤول الأول عبر /setup.php
SETUP_ENABLED=true
